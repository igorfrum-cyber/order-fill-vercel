{
  "name": "order-fill-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3200",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 3200",
    "test:workbook": "node scripts/test-workbook.mjs",
    "verify": "npm run test:workbook && npm run build",
    "precommit": "npm run verify"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "fflate": "^0.8.3",
    "vite": "^7.2.4",
    "xlsx": "^0.18.5",
    "xpath": "^0.0.34"
  }
}
